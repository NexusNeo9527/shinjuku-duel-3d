[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# single-page app: serve index.html for any path
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
